"""
Views for the metadata app.

All metadata API endpoints are in metadata/api/views.py

This module is reserved for future template-based views, such as:
    - Metadata dashboard
    - Run history pages
    - Schema visualization

For REST API endpoints, see metadata.api.views
"""
# Template-based views can be added here as needed
